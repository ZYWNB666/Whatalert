# 监控告警系统 - Web 前端

基于 Vue 3 + Element Plus 的现代化管理界面。

## 技术栈

- **框架**: Vue 3 (Composition API)
- **UI 库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP 客户端**: Axios
- **构建工具**: Vite
- **样式**: SCSS

## 功能页面

- ✅ 用户登录认证
- ✅ 概览仪表板
- ✅ 告警规则管理（CRUD）
- ✅ 当前告警列表（自动刷新）
- ✅ 历史告警查询
- ✅ 静默规则管理
- 🚧 数据源管理（占位）
- 🚧 通知渠道管理（占位）
- 🚧 用户管理（占位）
- 🚧 审计日志（占位）

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问: http://localhost:3000

### 生产构建

```bash
npm run build
```

构建产物在 `dist` 目录。

## 项目结构

```
web/
├── src/
│   ├── api/              # API 接口封装
│   │   ├── request.js   # HTTP 客户端
│   │   ├── auth.js      # 认证 API
│   │   ├── alertRules.js # 告警规则 API
│   │   └── silence.js   # 静默规则 API
│   ├── layout/           # 布局组件
│   │   └── MainLayout.vue
│   ├── router/           # 路由配置
│   │   └── index.js
│   ├── stores/           # 状态管理
│   │   └── user.js
│   ├── views/            # 页面组件
│   │   ├── Login.vue
│   │   ├── Dashboard.vue
│   │   ├── AlertRules/
│   │   ├── Alerts/
│   │   └── ...
│   ├── App.vue           # 根组件
│   └── main.js           # 入口文件
├── index.html            # HTML 模板
├── package.json          # 依赖配置
├── vite.config.js        # Vite 配置
└── README.md             # 本文件
```

## 默认账户

- 用户名: `admin`
- 密码: `admin123`

登录后请及时修改密码！

## 开发说明

### API 代理配置

开发环境下，Vite 会自动代理 `/api` 到后端服务（默认 `http://localhost:8000`）。

修改后端地址请编辑 `vite.config.js`:

```js
server: {
  proxy: {
    '/api': {
      target: 'http://your-backend-url',
      changeOrigin: true
    }
  }
}
```

### 添加新页面

1. 在 `src/views/` 创建组件
2. 在 `src/router/index.js` 添加路由
3. 在 `src/api/` 添加 API 封装（如需要）

## 样式规范

- 使用 Element Plus 默认主题
- 深色侧边栏: `#001529`
- 使用 SCSS 编写自定义样式
- 响应式设计

## 更多文档

详细文档请查看: [../docs/](../docs/)
